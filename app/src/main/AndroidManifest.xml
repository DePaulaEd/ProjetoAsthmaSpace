<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões para Internet e Localização -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />




    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoAsthmaSpace"
        android:usesCleartextTraffic="true">

        <receiver android:name=".Domain.LembreteReceiver" />



        <!-- Activity de Login é a tela de início -->
        <!--        android:name=".AppStart"-->
        <!--Vai ser a activity principal, porem agora para testes nao -->
        <activity
            android:name=".ActivityView.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras Activities do fluxo de cadastro -->
        <activity
            android:name=".ActivityView.Cadastrar"
            android:exported="true" />
        <activity
            android:name=".ActivityView.InfAdicionais"
            android:exported="true" />

        <activity android:name=".ActivityView.InformacoesPessoais" android:exported="false" />

        <activity android:name=".ActivityView.EditarInformacoes" android:exported="false" />



        <!-- Activity Principal (com os fragmentos) -->
        <activity
            android:name=".ActivityView.MainActivity"
            android:exported="false" />

        <activity android:name=".ActivityView.AjudaActivity" />
        <activity android:name=".ActivityView.DiarioSintomasContainerActivity" />
        <activity android:name=".ActivityView.Perfil" />


        <!-- FileProvider para compartilhar arquivos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>